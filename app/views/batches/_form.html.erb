<script type="text / javascript ">
$(function() {
$( "#datepicker " ).datepicker();
});
</script>

<%= form_for(@batch) do |f| %>
  <% if @batch.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@batch.errors.count, "error") %> prohibited this batch from being saved:</h2>

      <ul>
      <% @batch.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= collection_select(:batch, :user_id, @users, :id, :name, {:prompt => true}) %>
  </div>
  <div class="field">
    <%= f.label :style_id %><br />
    <%= collection_select(:batch, :style_id, @styles, :id, :name, {:prompt => true}) %>
  </div>
  <div class="field">
    <%= f.label :brew_date %><br />
    <%= f.text_field :brew_date %>
  </div>
  <div class="field">
    <%= f.label :rack_date %><br />
    <%= f.text_field :rack_date %>
  </div>
  <div class="field">
    <%= f.label :bottle_date %><br />
    <%= f.text_field :bottle_date %>
  </div>

  <div class="field">
    <%= f.label :comments %><br />
    <%= f.text_area :comments %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
